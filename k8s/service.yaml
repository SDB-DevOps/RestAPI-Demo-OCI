apiVersion: v1
kind: Service
metadata:
  name: flask-app-python-service
spec:
  selector:
    app: flask-app-python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
